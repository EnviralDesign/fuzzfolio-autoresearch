# V38 component-surrogate validation V2

## Current disposition

`p3_semantic_authority_recovered_component_canary_complete_pending_pro_review`

The durable scope archive recovered the exact isolated panel-3 year-1-q3 bar root. The human semantic-authority ruling accepts its frozen semantic identity after two local recomputations; receipt hashes remain provenance rather than a same-value requirement. A one-component, two-pass indicator/event canary subsequently completed against those isolated bars. It is a sealed feature projection only, not a graph replay, strategy simulation, or outcome/economic evaluation. Pro review is the stopping boundary before any further window or outcome work.

## What was established

- AutoResearch source: `51c2f9175f441166e7fc997109e939a9f9103b5d`.
- Historical Trading-Dashboard engine: `2bd50ccb3af1700d286da88cbcaecb4aca24f1a2`.
- V38 campaign: `temporal-qd-v5-fast-ephemeral-operator-family-matrix-20260820-v38`.
- The source boundary for an allowed component-only calculation is `backend/app/api/scoring/indicator_preview.py` in the pinned engine: it instantiates an indicator and calculates it from historical OHLCV without a strategy profile, graph, trade simulation, or economics.
- `autoresearch/signal_atlas.py` is not an extractor seam: its path invokes replay/simulation and writes a forward-event sidecar, so it was not used.
- The sealed numeric contract is `feature-protocol-v2.json`, commit `0a5a74d`, SHA-256 `b412209520f0a1b8ea7bacf0d1f0e0bef1eda508f0e8096fdb8e31bc8b8c04cc`.
- `scope-archive-semantic-authority-ruling-v1.json` records the accepted panel-3 semantic identity and the distinct historical/recovery receipt hashes.
- `feature-protocol-v2-terminology-addendum-v1.json` binds `eventStartShareOfActiveBars` and records that runtime `freshEventAvailability` is unavailable in component-only execution.
- `p3-component-only-canary-report-v1.md` records the deterministic two-pass component projection and its source/guard identities.

## Archived-bar and canary result

The mounted current lake remains unsuitable and was not used. The isolated archive root supplied five M5 files for panel-3 year-1-q3, each matching its frozen raw artifact hash. Two fresh-process executions of the historical `PRICE_RECLAIM_MA` component produced identical 18,978-row feature projections with canonical payload SHA-256 `d3067eb37d5a12bfd62c1212186771ebe3ddb6bfd461aa4909335b5262984f6c`.

The detailed recomputation and canary artifacts remain ignored under `.tmp/artifacts/component-surrogate-validation-v2/` and are included in the Pro review packet. No strategy replay, child evaluation, worker/gateway/Vast operation, calibration, generation, outcome analysis, or policy change occurred.

## Authorized resumption condition

Do not continue into the remaining windows or outcome phase without a new human/Pro decision. Any resumption must retain the same frozen task identities, semantic-digest implementation, isolated archive boundary, component-only seam, and separation from strategy/outcome execution.
